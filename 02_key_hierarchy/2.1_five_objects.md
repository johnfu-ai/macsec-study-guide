## 2.1 五个密钥对象：CAK/CKN/KEK/ICK/SAK

初学 MACsec 最常见的困惑，是五个缩写在文档里满天飞却分不清彼此。这一节先把全景表摆出来，再点出三个最容易记错的细节。记住一条主线：PSK 预共享的是 **CAK + CKN**，不是 SAK；CAK **从不**加密用户帧；KEK / ICK 由两端各自用同一套 KDF 算出来；SAK 由 Key Server **另生成**一把，再用 KEK 封装后经 MKA 分发。

### 2.1.1 对象总表

| 对象 | 是什么 | 怎么来 | 干什么 |
|---|---|---|---|
| **CKN** | CAK 的名字，不是密钥 | PSK：配置（本实验室 ASCII `MACSEC-LAB-CKN01`）。EAP：`KDF(MSK[0:N], "IEEE8021 EAP CKN", Session-ID‖mac1‖mac2, 128)` | MKPDU Basic；KDF 的 16 字节 context |
| **CAK** | 连通联盟根密钥 | PSK：预共享。EAP：`KDF(MSK[0:N], "IEEE8021 EAP CAK", mac1‖mac2, N×8)` | **只**派生 KEK / ICK |
| **KEK** | 封装 SAK 的钥匙 | `KDF(CAK, "IEEE8021 KEK", CKN[0:16], 128)`，两端各自算 | `AES-KeyWrap` → Distributed SAK |
| **ICK** | MKPDU 完整性钥匙 | `KDF(CAK, "IEEE8021 ICK", CKN[0:16], 128)`，两端各自算 | `AES-CMAC` → MKA ICV |
| **SAK** | 用户帧 GCM 钥匙 | Key Server **随机生成**（不是从 CAK 算），经 MKA 分发 | SecY 保护数据面；换钥再发一把 |

### 2.1.2 最容易记错的三件事

**PSK 预共享的不是 SAK**。两端事先配好的只有 CAK（根密钥）与 CKN（它的名字）。保护用户流量的 SAK 是会话建立后才由 Key Server 生成的——这也是为什么抓 PSK 握手包看不到任何可以直接解流量的材料。

**SAK 不是 `KDF(CAK, …)`**。它由 Key Server 随机生成，与 CAK 之间没有派生关系。线上出现的是 `AES-KeyWrap(KEK, SAK)`，不是 SAK 明文——这也是后面 [2.4 两条平面](2.4_two_planes.md)里"控制面封装、数据面使用"的分工。

**CKN 是名字，不是密钥**。它出现在 MKPDU Basic 参数集里明文可见，同时充当 KDF 的 context。泄露 CKN 本身不泄露机密，但会暴露"这是哪一个联盟"。

### 2.1.3 在抓包与代码里找到它们

PSK 抓包：[`captures/mka-handshake.pcap`](../captures/mka-handshake.pcap)。EAP 成功之后：[`captures/mka-after-eap.pcap`](../captures/mka-after-eap.pcap)（从 EAP-Success 起；完整 EAP-TLS 在 [ieee-802.1x-study-guide](https://github.com/johnfu-ai/ieee-802.1x-study-guide)）。

实现代码：[`macsec_lab/crypto.py`](../macsec_lab/crypto.py)（`derive_kek` / `derive_ick` / `derive_eap_cak` / `derive_eap_ckn` / `wrap_sak`）。演示值：[`captures/keys.json`](../captures/keys.json)（公开，勿用于真实链路）。
